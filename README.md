
  # SaaS Web App Prototype

  This is a code bundle for SaaS Web App Prototype. The original project is available at https://www.figma.com/design/IRdsixV97LMimMyqeYDj0D/SaaS-Web-App-Prototype.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  